<%= simple_form_for(@event) do |event| %>
  <%# <% event.input :creator_id, :value => current_user.id %> %>
  <%= event.input :location, :placeholder => 'Where will your event be?' %>
  <%= event.input :description, :placeholder => 'What\'s your event about?' %>
  <%= event.input :date %>
  <%= event.submit 'Create event' %>
<% end %>
