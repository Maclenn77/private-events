<% if logged_in? %>

<h4> <%="Events created by you: " %> </h4>
<% @events.each do |event| %>
<div class="card mb-2 " style="width: 18rem;">
  <div class="card-body">
  <h5 class="card-title"><%= "You created an event in #{event.location} " %> </h5>
  <p class="card-text"> <%= "at #{event.date}." %></p>
  </div>
</div>
<% end %>

<h4> <%="Events that you were invited to: " %> </h4>

    <%  %></h5>

<% @invited_to.each do |inv| %>
<div class="card" style="width: 18rem;">
  <div class="card-body">
  <h5 class="card-title"><%= "* You were invited to an event in #{Event.find_by(id: inv.event_id).location } " %> </h5>
  <p class="card-text"> <%= "at #{Event.find_by(id: inv.event_id).date.to_formatted_s(:rfc822)}.  Will you go?" %></p>
    <div class="custom-control custom-switch">
  <input type="checkbox" class="custom-control-input" id="customSwitches">
  <label class="custom-control-label" for="customSwitches">Toggle this switch element</label>
</div>
  </div>
</div>

  <!-- Default switch -->

<% end %>
<% end%>
